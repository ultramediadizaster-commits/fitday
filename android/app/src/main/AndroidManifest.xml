<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <!-- Оятқыш дабылын қабылдайды. exported=false: тек өз AlarmManager-імізден
             келеді, сырттан ешкім соқтыра алмайды. -->
        <receiver
            android:name=".AlarmReceiver"
            android:exported="false"
            android:directBootAware="true" />

        <!-- Телефон қайта қосылғанда оятқышты қалпына келтіреді.
             AlarmManager-дегі дабылдар өшірген кезде жоғалады, ал баптау
             SharedPreferences-те тұр. -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true"
            android:directBootAware="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
            </intent-filter>
        </receiver>

        <!-- Оятқыш қызметі: дыбысты оятқыш ағынында ойнатады және соғар
             алдында процесті тірі ұстайды.
             mediaPlayback — соғу кезінде (шынымен дыбыс ойнатылады),
             specialUse — дайындық кезеңі үшін. -->
        <service
            android:name=".AlarmService"
            android:exported="false"
            android:stopWithTask="false"
            android:foregroundServiceType="mediaPlayback|specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="alarm" />
        </service>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- ОЯТҚЫШ.
         USE_EXACT_ALARM — нағыз оятқыш қосымшалары үшін, Android 13+ жүйесінде
         автоматты беріледі (пайдаланушыдан сұрамайды). Ескі нұсқалар үшін
         SCHEDULE_EXACT_ALARM қалады — оны пайдаланушы баптаулардан береді.
         Екеуі де болмаса, дәл уақытта соғу кепілдігі жоғалады.
         Ескерту: USE_EXACT_ALARM-ды Google Play тек оятқыш/күнтізбе
         қосымшаларына рұқсат етеді — жариялағанда себебін көрсету керек. -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- Android 13+ хабарлама көрсету рұқсаты (пайдаланушыдан сұралады) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Дірілдеу және телефон қайта қосылғанда оятқышты қалпына келтіру -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Құлыпталған экранда оятқыш бетін ӨЗІ ашу үшін (басудың қажеті болмасын) -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <!-- Оятқыш қызметі. Android 14+ түрге сай рұқсатты талап етеді:
         дыбыс ойнату — MEDIA_PLAYBACK, соғар алдындағы дайындық — SPECIAL_USE. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- Doze режимі процесті ұйықтатып, оятқышты кешіктірмеуі үшін.
         Пайдаланушыдан жүйелік диалог арқылы сұралады. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
</manifest>
