<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- ОЯТҚЫШ.
         USE_EXACT_ALARM — нағыз оятқыш қосымшалары үшін, Android 13+ жүйесінде
         автоматты беріледі (пайдаланушыдан сұрамайды). Ескі нұсқалар үшін
         SCHEDULE_EXACT_ALARM қалады — оны пайдаланушы баптаулардан береді.
         Екеуі де болмаса, дәл уақытта соғу кепілдігі жоғалады.
         Ескерту: USE_EXACT_ALARM-ды Google Play тек оятқыш/күнтізбе
         қосымшаларына рұқсат етеді — жариялағанда себебін көрсету керек. -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- Android 13+ хабарлама көрсету рұқсаты (пайдаланушыдан сұралады) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Дірілдеу және телефон қайта қосылғанда оятқышты қалпына келтіру -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
</manifest>
